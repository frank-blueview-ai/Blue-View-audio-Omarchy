# Audio E2E evidence — 2026-09-13

## Current status

Session wrap-up: the user confirmed that only the built-in microphone is available and agreed to finish for now. Precise per-driver frequency calibration is deferred; no automatic cutoffs or calibration EQ were applied. The published code, documentation, and measurements represent the completed work for this session. The validation limits below remain documented.

Audio is audible again: the user confirmed all four cues alternating left/right, and the microphone acoustic probe passed every lane. Camera capture and preview delivered real frames; camera power was restored off. The final controls add independent balance/fade, retain individual mutes and microphone selection/volume, move volume controls above device selection, put EQ bands above the visualizer, and collapse Four-channel routing by default.

This record includes earlier failed investigations followed by corrections. The latest results supersede those earlier observations. Reboot/suspend amplifier recovery and a full browser-camera workflow have not been validated; Dolby Atmos activation is unavailable on this Linux audio path. The four PCM lanes do not establish four independent physical surround positions in the iMac's stereo enclosure.


Machine: iMac14,4, PipeWire 1.6.8, WirePlumber 0.5.17, Cirrus CS4208.

## Hardware topology caveat

Apple's specification for this iMac identifies its built-in audio as stereo speakers, dual microphones, and a headphone/optical-output jack ([Apple technical specifications](https://support.apple.com/en-us/112031)). The user identified the built-in speakers as the current output. Linux exposes the internal CS4208 output as `analog-surround-40`, and the HDA codec dump has two fixed digital Speaker pins (`0x1d`, `0x1e`) connected to two multichannel digital converters (`0x0a`, `0x0b`). Four active PCM/monitor lanes therefore do not, by themselves, establish four independent surround speaker positions. The saved front/rear-exchange map was listener-confirmed earlier and has been preserved; the 4.0 software profile and the iMac's published stereo hardware description do not settle how the user's cues map acoustically.

A temporary, non-persistent pair probe created a second remap sink with physical lane order preserved and channel layout `[FL, RL, FR, RR]`. A low-level 440 Hz left cue sent to the first two PCM lanes measured `−41.6 dBFS` on hardware FL/FR and digital silence on RL/RR; an 880 Hz right cue sent to the second two lanes measured `−41.6 dBFS` on RL/RR and silence on FL/FR. The temporary sink was unloaded afterward; default output, saved routes, mutes, and EQ values remained unchanged. This proves the proposed left/right lane grouping at the PCM layer only; the mic check below provides acoustic presence data but cannot identify physical placement.

## Speaker path and individual mutes

An audit found that earlier `pw-cat --record --target=...monitor` tests were not reliably bound to the requested monitor. WirePlumber connected one such capture to the built-in microphone. Measurements from those captures are superseded below; they are not evidence of hardware output.

The current tests record with `parec` explicitly selecting `alsa_output.pci-0000_00_1b.0.analog-surround-40.monitor`, `--fix-format`, `--fix-rate`, `--fix-channels`, and `--no-remix`. During capture, `pactl list source-outputs` confirmed source index `19941`, four-channel `s32le` at 48 kHz, and the monitor stream name.

A low-level four-channel test sent a different tone on each input lane through `imac_fixed`. The hardware monitor measured the saved front/rear exchange exactly:

| Logical cue | Physical output lane | Monitor RMS |
| --- | --- | ---: |
| Front Left | Rear Left | `−48.2 dBFS` |
| Front Right | Rear Right | `−47.8 dBFS` |
| Rear Left | Front Left | `−44.5 dBFS` |
| Rear Right | Front Right | `−44.0 dBFS` |

The repeatable check `python3 tests/e2e_speaker_channels.py --run` played the system's spoken `Front Left`, `Front Right`, `Rear Left`, and `Rear Right` WAV cues one at a time through `paplay` and the same 4-channel route. The explicitly selected ALSA monitor recorded only the expected hardware lane for each cue: `Front Left → Rear Left` (`−37.9 dBFS`), `Front Right → Rear Right` (`−38.4 dBFS`), `Rear Left → Front Left` (`−37.1 dBFS`), and `Rear Right → Front Right` (`−36.0 dBFS`); each other lane was silent in its cue window. Thus all four digital hardware lanes carried signal at test time. The later microphone check below adds acoustic evidence, but does not establish four independent physical speakers or their locations on this iMac.

The repeatable check `python3 tests/e2e_speaker_mutes.py --run` exercised each physical-speaker mute in turn while recording that same monitor. All four lanes began at `−29.5 dBFS`; muting the selected lane took it to digital silence (`−240 dBFS`), while the other three stayed live at `−29.5 dBFS`. Each channel returned to `−29.5 dBFS` immediately after unmuting. The test confirmed all four persisted mute states ended off, the default stayed `imac_fixed`, the hardware master and output-stream levels were restored, and the route map and EQ state were unchanged.

PipeWire’s standard phase-difference stereo upmix is active for PulseAudio, native PipeWire, and ALSA-default clients (`channelmix.upmix=true`, `channelmix.upmix-method=psd`, `channelmix.rear-delay=12.0`). The earlier `simple` override was removed. `python3 tests/e2e_stereo_upmix.py --run` records a quiet mono tone and a distinct left/right stereo tone through `paplay`, native `pw-cat`, and `aplay -D default`. The mono tone reaches only its mapped front pair (`−41.5` to `−41.6 dBFS RMS` on raw RL/RR, silence on FL/FR under the saved route); PSD has no rear-difference signal for identical mono channels. Each stereo test reaches all four hardware lanes at `−41.5` to `−41.6 dBFS RMS`. The test refuses to run when a real application stream is attached or when a speaker mute is active.

PipeWire's JACK client config exposes the selected default as conventional `system:playback_N` ports. `python3 tests/e2e_jack_multichannel.py --run` connected four separate JACK outputs to those ports; the monitor recorded the same confirmed map as the Pulse test: `Front Left → Rear Left`, `Front Right → Rear Right`, `Rear Left → Front Left`, and `Rear Right → Front Right`, each at `−41.5 dBFS`, with other lanes silent for each cue. A separate two-output JACK probe connected only `system:playback_1/2` and produced signal only on the mapped Rear Left/Rear Right lanes; direct JACK port links do not invoke the stereo-to-four-speaker upmixer. Four-channel JACK routes are verified; stereo JACK upmix remains unsupported by this graph.

`python3 tests/e2e_jack_eq.py --run` kept one four-port JACK node connected during a temporary `+6 dB` 2 kHz change and bypass. Each physical lane moved from `−41.5` to `−35.5 dBFS` (`+6.0 dB`) and returned to `−41.5 dBFS`. The same JACK node and links remained through both EQ updates; the test restored the flat bypass, saved slider values, routing map, and default output.

These earlier measurements verify the signal path through the hardware sink. At the time of the Front Right-only report, PipeWire exposed no non-test music stream (only its silent `/dev/zero` keepalive); Chromium's MPRIS player reported paused. The live player was later observed and traced as documented below.

After the latest Front Right-only report, I repeated `python3 tests/e2e_stereo_upmix.py --run` with both mono and stereo inputs and `python3 tests/e2e_speaker_channels.py --run`. Mono reached the mapped front pair and stereo reached all four lanes for PulseAudio, native PipeWire, and ALSA-default clients. The spoken cues again reached only their selected mapped monitor lanes (Front Left → Rear Left, Front Right → Rear Right, Rear Left → Front Left, Rear Right → Front Right). At this retest, every output mute was off, all four Blue View output levels were 84%, the hardware sink master was 100%, and the default remained `imac_fixed`. No non-test music stream appeared during that retest; the later live-stream measurement below confirms all four digital lanes carry the user's music.

## Equalizer and live-stream continuity

The opt-in check `python3 tests/e2e_eq_multichannel.py --run` kept one four-channel `paplay` stream attached while it switched the live PipeWire EQ from flat to a temporary `+6 dB` 2 kHz band and back to flat. Its stream id stayed present through both changes. The monitor measured each lane at `−41.5 dBFS` for the flat 250 Hz and 2 kHz tones, then `−41.4 dBFS` at 250 Hz and `−35.5 dBFS` at 2 kHz with the band enabled (`+5.9 dB`); after bypass, 2 kHz returned to the original level (`0.0 dB` difference). The original device-state file, speaker routes, and EQ sliders were unchanged. The test restored the graph to bypass because the saved profile had not been active before the test.

The graph now includes an identity-only linear probe alongside PipeWire's multichannel `param_eq` node. That probe confirms that the live graph exists; the runtime check compares its node serial and saved graph hash rather than searching `Props` for graph text, which PipeWire does not return. Applying an EQ change updates the live node directly and leaves playback streams connected.

## Playback continuity

`python3 tests/e2e_output_handoff.py --client pulse --run`, `--client pipewire --run`, and `--client alsa --run` each played one continuous quiet stream while switching between `imac_fixed` and Open Quad Spatial, then switching back. The same stream ID survived each handoff for all three APIs. Through the built-in route and after return, all four hardware-monitor lanes measured approximately `−41.5 dBFS`; the temporary Open Quad stage also retained signal on all lanes. The 100 ms minimum output windows stayed between `−50.3` and `−44.9 dBFS` during the switches. Default output, master volume/mute, saved routing/EQ/mute/spatial state, and the saved device-state file all passed restoration checks for each API.

A second physical output could not be tested because the HDMI profiles and headphone output are unavailable. These earlier handoff tests used synthetic streams; the live Chromium stream was inspected in the later check below.

## Media-player compatibility

`python3 tests/e2e_mpv_playback.py --run` played quiet mono and stereo WAV signals through MPV's PipeWire, PulseAudio, and ALSA backends. Each mono test reached the two lanes selected by the saved logical front-pair routes (`−41.9` to `−45.1 dBFS RMS`); each stereo test reached all four hardware lanes (`−42.3` to `−43.1 dBFS RMS`). The helper confirmed the saved routes, EQ values, mute flags, and default output were unchanged. Chromium had no PipeWire playback stream during that earlier MPV check; the live browser stream was later observed and measured below.

## Live browser stream and microphone check

With the user's YouTube music playing, Chromium sink-input `30401` was attached to `imac_fixed`, uncorked, and unmuted at `128%`. It stayed the same stream through the live routing check. With explicit four-channel capture binding, the hardware monitor measured `−41.1`, `−41.1`, `−36.0`, and `−37.2 dBFS RMS` on its four channels, with every 1-second combined window between `−44.8` and `−35.7 dBFS`. This confirms signal on all four digital output lanes while the browser stream was active.

During a live route rebuild, the same Chromium stream remained uncorked and unmuted on the destination sink. The migration took `8.77 s`; all `81` consecutive 100 ms monitor windows contained signal, none fell below `−90 dBFS`, and the quietest window measured `−52.4 dBFS`. The default remained `imac_fixed`, and the saved front/rear exchange and channel mute settings were preserved.

An initial three-second mic comparison showed a modest `2.3 dB` rise during playback, but a later check below did not reproduce it. No microphone recording was saved; only signal-level metrics were kept.

A separate low-level, one-at-a-time 440 Hz cue check at `−30 dBFS` confirmed the saved logical-to-digital-lane map on the hardware monitor: Front Left → Rear Left, Front Right → Rear Right, Rear Left → Front Left, and Rear Right → Front Right, with non-selected lanes silent per cue. The mic's narrowband levels were near its noise floor for Front Left (about `3 dB` above baseline), higher for Front Right and Rear Left (about `8–12 dB` above baseline), and strongest for Rear Right (about `19–21 dB` above baseline). That means acoustic energy was detectable during the cues, but the weak Front Left pickup is inconclusive. Because this iMac's built-in speakers are specified as stereo, four active digital lanes do not prove four independent physical speaker positions or establish their acoustic locations. Physical speaker placement and the weak Front Left response still need an in-person listening check.

## Microphone and camera

The built-in analog microphone remains the default at `52%`, unmuted. Selecting it through Omarchy’s input helper preserved its volume and mute state. The panel retains Omarchy’s source selector and volume/mute controls, with a live level meter bound to the active source.

The FaceTime HD camera (`05ac:8511`) is available and currently disabled (`authorized=0`). The control wrapper’s on/off readback path was exercised with a temporary sysfs file and mocked privilege helper; the physical camera was left disabled.

## Current machine state

- Default output: `imac_fixed`; virtual and hardware sink masters at `100%`, unmuted.
- Blue View hardware-output stream: `79%` on all four channels at the latest check.
- Default microphone: built-in analog stereo, `52%`, unmuted.
- Camera: available and off.
- Speaker routing: saved front/rear exchange shown in the table above; all four channel mutes off. All four digital lanes carry cues and live browser signal. The latest mic comparison did not verify that current music is emerging acoustically; four independent physical speaker positions and the weak Front Left pickup also need in-person confirmation.
- Saved EQ: bypassed; front gains `[12, 0, 3, 11, 0, 12]`, rear gains `[12, 0, 0, 12, 0, 12]` remain stored unchanged. The live four-channel graph is present and flat (`eqApplied=true`); the separate +6 dB response test confirms the multichannel DSP works when enabled.
- Open Quad Spatial: off.
- HDMI and headphone outputs are unavailable. Live Chromium stream continuity and digital output on all four lanes are verified. Acoustic output from the current music remains unverified; no claim of four independent built-in speakers is made.

## Follow-up after the user still reported silence

The current Chromium sink-input `33802` was unmuted and uncorked at `128%` on `imac_fixed`. A fresh, explicitly bound hardware-monitor capture measured `−31.1`, `−31.1`, `−24.9`, and `−26.9 dBFS RMS` on the four hardware channels; the three 1-second combined windows were `−27.0`, `−28.1`, and `−28.0 dBFS`. Thus substantial signal is reaching the HDA digital output path.

The speaker route is selected (`analog-output-speaker`). ALSA `Master` and `PCM` are both `100%` and on; HDA power saving is disabled. Codec converters `0x0a` and `0x0b` were in D0 with stream tag `5`, speaker pins `0x1d` and `0x1e` were set to output, and GPIO 0 was enabled/high. However, the kernel log says it selected a blank CS4208 fixup for subsystem `106b:0000`. Linux documents a CS4208 `gpio0` model for enabling the amp, and a separate iMac16,1 kernel patch describes that model's external amplifier arrangement ([CS4208 model documentation](https://github.com/torvalds/linux/blob/master/Documentation/sound/hd-audio/models.rst), [iMac16,1 kernel patch](https://lkml.rescloud.iu.edu/hypermail/linux/kernel/2605.3/04191.html)). GPIO 0 is already high here, so simply setting that bit again is not an evidenced fix.

For this current stream, the built-in mic measured `−62.0/−62.4 dBFS RMS` during playback and `−56.8/−57.3 dBFS` during a temporary mute. This did not show music-related acoustic energy above the changing room-noise floor. The stream was restored unmuted and uncorked. The evidence therefore places the remaining fault after the software mixer and HDA monitor: likely the model-specific CS4208-to-speaker-amplifier path or the physical amp/speaker connection. The applet cannot confirm which without a successful acoustic test.


## Controlled acoustic isolation and corrected driver interpretation

The earlier inference from the blank fixup log is withdrawn. In the upstream driver, `cs4208_fixup_mac()` explicitly falls back to `CS4208_GPIO0` when lookup finds no model entry, then applies that fixup. The blank diagnostic is not proof that amplifier initialization was skipped; the local GPIO-0 high state is consistent with the fallback. See the [upstream CS420x driver](https://github.com/torvalds/linux/blob/master/sound/hda/codecs/cirrus/cs420x.c). The firmware `init_pin_configs` already assigns speakers to `0x1d`/`0x1e`; `driver_pin_configs` and `user_pin_configs` are empty. No pin override was found.

The new opt-in `e2e_acoustic_probe.py` ran through Blue View and directly through the hardware sink. It verified the actual mic and hardware-monitor source IDs, native S32LE sample formats, channel maps, and unmuted captures. Capture latency is explicitly 20 ms; older default `parec` captures advertised two-second buffering, so process-start timing was not a reliable acoustic alignment reference. The new probe identifies the cue intervals from the monitor signal, uses the steady interior of each interval, and checks the 1 kHz tone against both silent intervals and neighboring frequency bins.

At the final test's -24 dBFS peak stimulus, every lane carried the expected digital cue: -33.2 dBFS tone RMS through Blue View and -27.0 dBFS directly through the hardware sink. Neither route produced a clear corresponding mic tone. Mic tone rises were -3.2 to +2.4 dB, with all channels below the required 15 dB separation from both baseline and neighboring bins. The acoustic gate is **inconclusive**, not passed. This supports further investigation below the applet but does not distinguish a physical speaker/amp problem, codec initialization, or microphone-path limitations.

Chromium's original mute/cork/volume state, all sink/source mute and volume states, the selected default, and saved device state passed restoration checks. Raw mic audio was kept in memory and discarded. [Structured metrics](evidence/acoustic-isolation-2026-09-13.json) contain only measurement results.

The camera monitor component now provides a detachable preview, video-device selection, pause/resume, frame count, and close-to-stop capture behavior. A hidden Quickshell runtime smoke test loaded Qt Multimedia successfully and confirmed capture inactive with zero frames while closed and hardware-disabled. Real camera frames and hardware power switching are still unverified because the USB camera remains disabled.


## Installed camera monitor and direct-move correction (12:55 local)

Installed `CameraMonitor.qml` and the updated panel into the user-owned `fperez.audio` plugin, with backups under `~/.local/state/blueview-audio/backups/20260913-125329-camera`. The panel now initializes camera state to disabled until device status arrives. The running shell logged the plugin reload without new QML load errors. An independent runtime check against the installed component showed `CAMERA_DISABLED_VISIBLE true false 0` and, after closing, `CAMERA_CLOSED false false 0`. This proves window lifecycle and disabled capture behavior, not real-camera frame delivery. The USB camera remains disabled. Unit checks passed 16/16 and `git diff --check` passed.

An ad-hoc Chromium bypass attempted with `pactl move-sink-input` reported success but left stream 33802 disconnected (`sink=4294967295`). Therefore the earlier claim that this ad-hoc test restored the route was premature, and its listening result cannot establish a successful bypass. Recovery used the existing helper's `migrate_sink_input_ids` method, which clears legacy target metadata, uses the virtual sink serial, and verifies destination attachment. A subsequent independent `pactl` check confirmed Chromium 33802 on `imac_fixed` 31925. Future live handoffs must use that verified method rather than relying on `pactl` exit status. No audio services were restarted and saved routing/EQ state was not changed.


## Centered-audio rear-fill correction

The new isolated `e2e_centered_fill.py --run` regression passed against a temporary four-channel null sink. With a known mono or identical-left/right 1 kHz input, PSD produced front signal and silent rear lanes. Applying `simple` at runtime produced signal on all four lanes for both inputs. See `evidence/centered-fill-2026-09-13.json`. The probe validates its capture binding and removes its temporary sink; it does not play through hardware or capture a microphone.

Updated the installed native-client and Pulse server stream configuration from PSD to simple fill, and applied the matching runtime parameter to the existing Chromium node 134. Readback confirmed `channelmix.upmix-method=simple`. Chromium stream 33802 remained connected to sink 31925; a subsequent live hardware-monitor capture measured -31.6, -32.4, -28.5, and -29.4 dBFS RMS. Saved EQ gains, speaker routes and mutes were not edited. Open Quad remains available separately. The Pulse server must still load its changed configuration during an idle restart before newly created Pulse streams inherit this default; the current Chromium stream already has it. Acoustic output remains unverified.


## In-person direct-output confirmation and device resume

Repeated the Chromium direct-output test using `migrate_sink_input_ids`, with destination verification before the 20-second listening interval and verified restoration afterward. Independent live readback showed stream 33802 on physical sink 19941, uncorked and unmuted. The user reported **Still silent** during this verified bypass. The silence therefore persists without the Blue View remap/EQ path; this does not identify a specific codec, amplifier, or hardware fault.

Then briefly suspended and resumed only the physical sink, always resuming in a finally block. ALSA reported RUNNING with a new trigger time (74643.150553686) and reset hardware pointer, confirming the playback device reopened. Chromium retained stream 33802 and its verified destination `imac_fixed` 31925. User listening confirmation after the resume is pending. No audio service restart, profile change, EQ change, or saved route/mute edit occurred.


## Physical camera validation and authentication recovery

The user confirmed that reopening the ALSA playback device still left music silent. Audio diagnosis remains unresolved below the verified application route.

Restarted only the Omarchy shell using its packaged command to recover the failed Polkit agent. The command's short startup deadline expired, but the new process remained alive; later logs confirmed agent registration and IPC `ping` returned `ok`. PipeWire and Chromium were not restarted; stream 33802 stayed attached to 31925.

With administrator authentication, the real camera USB authorization switched from 0 to 1 and `/dev/video0` appeared. `v4l2-ctl --stream-mmap=3 --stream-count=10 --stream-to=/dev/null` completed successfully with ten frames. The first preview attempt hit its 20-second process deadline during slow startup; this is not proof of camera failure. An open video descriptor in the main shell was also not proof of active capture: the direct capture test succeeded while it existed.

Changed CameraMonitor to create its Camera/CaptureSession through a Loader only while the preview is visible, enabled and unpaused. The installed component's subsequent live run exited successfully, reported `CAMERA_LIVE true 129` with no camera error, and then `CAMERA_CLOSED false 0`. Logs are in `evidence/camera-live-2026-09-13.log`; no video file was written. Afterward, authenticated hardware off succeeded and readback confirmed cameraEnabled=false, restoring the original disabled state. Unit checks remained 16/16. This validates real frame delivery and the closed preview's inactive state; device-selection alternatives and a full browser camera workflow still require testing.


## Direct ALSA isolation and amplifier-control recovery attempt

A first raw ALSA attempt failed with EBUSY: `pactl suspend-sink` was immediately undone while the live output link remained active. This attempted test produced no tones. For the actual test, temporarily set the PCH card profile off, waited for ALSA status `closed`, and played the four-channel stimulus through `aplay -D hw:1,0 -t raw -f S16_LE -r 48000 -c 4`. Aplay exited 0. The user reported **Still silent** for this successfully executed direct test. This excludes Blue View and PipeWire processing from that reproduction, but does not distinguish a driver/codec/amp fault from physical hardware.

Restored the exact card profile, sink/source levels and mutes, and defaults. Hardware sink/source IDs changed to 35188/35189. The Blue View output stream reconnected to 35188 and its EQ runtime marker remained valid. Chromium's old stream disappeared; MPRIS reported Paused. Resumed the browser through MPRIS Play and verified a new stream on `imac_fixed`. Therefore this exclusive hardware-profile test did not preserve uninterrupted application playback and is not an output-switch continuity pass.

Read the CS4208 GPIO state using authenticated hda-verb: mask=1, direction=1, data=1. Upstream `cs4208_fixup_gpio0` assigns bit 0 to speaker EAPD. A guarded, temporary recovery script checked vendor 0x10134208 and subsystem 0x106b7900 plus that exact GPIO state, muted the hardware output, cycled GPIO data 1→0→1 for 150 ms, and verified restoration to 1 before restoring the prior output mute. It did not alter GPIO mask/direction, codec coefficients, permanent configuration, or saved EQ/routing. The command succeeded; listening confirmation after this recovery attempt is pending. The camera remains disabled.


### Acoustic recovery confirmed, channel balance unresolved

After the guarded GPIO-0 cycle, the user confirmed audible browser music for the first time in this validation sequence, but reported that it came only from the right side. This is evidence of partial acoustic recovery, not a four-speaker pass. Began a single pass of four spoken logical channel cues through the saved Blue View map to determine left/right/silent correspondence, temporarily muting the existing Chromium stream and restoring its prior mute afterward. The mapping response is pending. Newly created Chromium streams still need the runtime simple-fill setting until the updated Pulse server configuration is loaded.

The spoken-cue process subsequently exceeded its 15-second deadline; its finally block restored Chromium mute. Do not treat this cue run as complete or use it as a digital channel-mapping pass.


## Confirmed four-channel acoustic recovery and persistent stream defaults

The user clarified the spoken cue result as **All four: left, right, left, right**, then confirmed that all speakers were audible during music. The cue process timeout remains recorded as a harness failure, but the user's acoustic observation is positive.

Reloaded only pipewire-pulse to load the saved simple-fill default. This deliberate maintenance operation recreates Pulse client streams and is not a continuity test. Restored sink levels/mutes, the remap output level, default output, current EQ graph, and browser playback state. The saved device-state file was byte-for-byte unchanged. A newly created Chromium stream (35680) reported `channelmix.upmix-method=simple` without a runtime override, confirming the default is now loaded for future Pulse streams.

Ran the controlled acoustic probe again at -24 dBFS peak. It exited 0 with `acoustic_gate=clear-tone-detected`: every logical channel was detected on both microphone channels through Blue View and directly through hardware. Through Blue View, tone energy exceeded baseline by 38.7–61.4 dB and neighboring frequency bins by 40.6–62.5 dB. The direct route also passed all channels. Application mute/cork and sink/source state restoration checks passed. No microphone recording was retained. See `evidence/acoustic-after-recovery-2026-09-13.json`. Right-side tones have higher mic pickup, which is not a calibrated listening-position balance measurement; the user independently confirmed correct left/right cues and audible music from all four.

This supersedes the earlier unresolved acoustic gate. Automatic recovery after a cold boot or system suspend is still untested; the GPIO cycle has only been validated as a one-time recovery on this iMac.

## Final controls and verification

Balance and fade attenuate the selected side/pair without boosting the opposite side. They work with EQ bypassed and do not rewrite EQ bands or per-channel mute baselines. The isolated continuous-playback test measured the expected gains of −6.02, 0, −12.04, and −6.02 dB for combined balance/fade, then restored all lanes to center. Unit tests: 19 passed. Python compilation, shell syntax, and git whitespace checks passed. Installed Panel.qml and control helper match the repository. A live panel screenshot confirmed the top slider layout; accordion behavior was inspected in source, not exercised through a mouse automation test.

The final mono/stereo checks passed through PulseAudio, native PipeWire, and ALSA. MPV passed both channel layouts through all three backends and preserved saved state/default output. The MPV test stimulus is −24 dBFS, with player volume explicitly 100%; user master sliders stay unchanged. At the existing master level, the earlier −34 dBFS MPV stimulus measured −75.43 dBFS on every lane, just below the old fixed −75 dBFS gate. That earlier threshold failure did not indicate missing channels.

The EQ test now measures restoration after the graph application completes, avoiding overlap with the processing transition. Final machine-readable results and command logs are in `evidence/final-checks-2026-09-13.json` and `evidence/final-*.log`. Tests pause browser playback for isolated measurements and resume it afterward; only the dedicated handoff test measures uninterrupted playback during switching. No microphone audio, camera frames, desktop screenshots, or account credentials are included in the repository.

### Completed playback gates and quality settings

All eight final gates passed: isolated balance/fade; mono/stereo desktop clients; MPV through three backends; four-channel EQ response and restoration; four independent mutes; and PulseAudio, native PipeWire, and ALSA output handoffs. Each handoff preserved the same playback stream and passed the 100 ms output-window gate. The final passing logs are summarized in `evidence/final-checks-2026-09-13.json`; that manifest points to the latest passing runs.

During verification, a nine-second mute stimulus ended before the final restored-channel measurement. Extending the stimulus to 30 seconds and checking that it remains alive resolved that harness defect without changing mute behavior. The output-switch tests also use a 30-second stimulus and −24 dBFS peak so the spatially attenuated lanes remain measurable at low user volume.

WirePlumber intermittently failed to create/activate links during a return to imac_fixed. Open Quad now accepts four input channels and blends original rear channels with the phase-derived fill. The migration helper also requests one fresh metadata rescan when a client is actually disconnected after port negotiation; completion still requires checking the destination sink. The final PulseAudio, native PipeWire, and ALSA runs passed the continuity gate. This is evidence for the observed configuration, not a guarantee about every external device/driver. The installer upgrades only the exact previous distributed spatial graph and retains a backup; custom graphs are preserved.

Hardware enumeration of the actual four-channel ALSA node reports S32_LE/S16_LE and 44.1–48 kHz. Runtime hw_params confirms S32_LE, four channels, 48 kHz. Thus this path already uses the maximum advertised word size/rate for this profile; the codec's wider rate list does not imply a four-channel 192 kHz option. See `evidence/hardware-quality-2026-09-13.txt`. Installed client/PulseAudio configuration selects resample.quality=14. The PulseAudio server was reloaded with saved levels, routing, EQ state, and browser playback restored.

Native microphone capture produced two channels of samples and measurable signal, but pw-cat returned exit 1 without an error message on both sample-count completion and intentional interruption. The evidence records that exit status; it is not a clean-process-exit pass. ALSA default capture returned 0 and 96,000 stereo frames. Neither test retained microphone audio. See `evidence/microphone-clients-2026-09-13.json`.

Quality 14 was confirmed on actual 44.1 kHz PulseAudio, native PipeWire, and ALSA playback streams. All four hardware lanes were active; each client passed 40 consecutive 100 ms windows during the steady interval. See `evidence/resampler-quality-2026-09-13.json`. The first recorder attempt omitted an explicit/fixed hardware channel map and returned misleading missing-lane data; the corrected recorder requests the four physical positions and fixes format/rate/channels. Native stream ownership is resolved through its client object as well as a direct process-id property.

The low-level 40 Hz–16 kHz per-channel survey completed, with saved controls unchanged and application mutes restored. All lanes met the tone-detection gate at 1, 2, 4, 8, 12, and 16 kHz. Lower-frequency detection varied by lane; these uncalibrated microphone results do not support automatic driver cutoffs or bass compensation. See [the survey table](speaker-response-2026-09-13.md) and `evidence/frequency-response-2026-09-13.json`. No calibration EQ was applied.

### Native microphone exit status explained

Inspected the [PipeWire 1.6.8 pw-cat source](https://github.com/PipeWire/pipewire/blob/1.6.8/src/tools/pw-cat.c), matching the installed package. The program initializes exit_code to EXIT_FAILURE and changes it to EXIT_SUCCESS only when data.drained is true. The on_drained callback sets that flag; the recording sample-limit branch and SIGINT handler instead quit the main loop directly. Thus the observed exit 1 on recording completion/interruption is explained by the utility's exit logic, not evidence of microphone capture failure. The two-channel sample counts and signal measurements remain valid. The evidence now records this interpretation explicitly.

Precise per-driver frequency limits remain unestablished. A related-model kernel patch describes separate tweeters/woofers on iMac16,1, but this machine is iMac14,4; that does not establish this machine's physical lane order. The listener-confirmed routing remains unchanged. Further calibration requires identified physical drivers and suitable acoustic measurement evidence; the current built-in-mic survey must not be used as an automatic cutoff map.
